import Button from 'file:///Users/rob/Sites/rsc/docwood/web/dist/rsc/docs/mdx/Button'

# MDX Tests

This is a regular paragraph.

This should be an inline expression showing the value of `Math.PI`: {Math.PI}

And we should be able to use components like this: <Button />
