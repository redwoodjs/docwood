export const Button = () => {
  return (
    <button
      type="button"
      className="text-sm rounded bg-blue-500 px-3 py-1 font-semibold text-white"
    >
      Click me!
    </button>
  )
}

# MDX Tests

This is a regular paragraph.

The MDX render should be using remark-breaks
so this should be broken onto two lines.
And a third line!

Inline expression `{Math.PI}` showing the value of π: {Math.PI}

Render a component: <Button />
